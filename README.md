# harness

`harness` is an agent harness which creates a consistent, repeatable, user-friendly experience when
using LLMs to augment any task.

This tool is primarily human-designed and human-written. AI was used to automate small, individual changes, but this is not a "vibe coded" project.
